/*
	var max = inputValue.length >= newInputValue.length ? inputValue.length : newInputValue.length;

	for(var i = 0; i < max; i++) {
		if(inputValue[i] != newInputValue[i]) {
			start = i;
			break;
		}
	}

	for(var i = 0; i < max - start + 1; i++) {
		if(inputValue[inputValue.length - 1 - i] != newInputValue[newInputValue.length - 1 - i]) {
			end = newInputValue.length - i;
			break;
		}
	}*/